---
name: skill-structure-reviewer
description: 审查 Agent Skill 的结构契约，重点检查术语一致性、步骤边界、执行路由和状态流转，用于发现命名漂移、隐式依赖、加载条件不清和跨步骤状态漂移。
---

# Skill 结构审查器

## 用途

审查其他 Agent Skill 的结构设计，只检查四个方面：

1. 术语一致性
2. 步骤边界
3. 执行路由
4. 状态流转

核心原则：

- 一个概念，一个标准名称。
- 一个步骤，一个清晰职责。
- 一个状态，一条明确路由。
- 跨步骤数据，必须有明确归属。

除非用户另行明确要求，否则不要执行、修改或重写被审 Skill。

## 审查范围

从目标 Skill 的 `SKILL.md` 开始，按需读取其引用文件。

不审查：业务知识、最终产物质量、代码质量、安全、Token 精确消耗、工具实现细节，以及与结构无关的文案风格。

## 执行流程

### 1. 读取入口

先读取目标 `SKILL.md`，识别：

- 主流程
- 步骤与引用文件
- 状态文件
- 分支条件
- 持久化产物

不要默认加载整个目录。

### 2. 按路由加载

根据已发现的信息决定下一步读取什么：

| 条件 | 加载内容 |
|---|---|
| 主流程引用某步骤 | 对应步骤文件 |
| 步骤使用跨步骤数据 | 对应状态定义或存储文件 |
| 同一概念疑似存在多个名称 | 涉及该概念的 reference / schema |
| 需要确认输入输出契约 | 相关 template / schema / reference |
| 与四类审查无关 | 不加载 |

每次加载后重新判断，不因文件存在就自动读取。

### 3. 建立四个内部模型

- 概念表：`概念 -> 实际使用名称`
- 步骤表：`步骤 -> 职责 / 输入 / 输出 / 依赖`
- 路由表：`状态或条件 -> 加载内容 -> 完成条件 -> 状态更新 -> 下一步`
- 状态表：`数据 -> 产生位置 -> 存储位置 -> 消费位置`

默认在当前上下文维护这些模型。目标 Skill 较大、需要分批读取或存在中断恢复需求时，将模型写入审查工作区的临时状态文件；不要写入目标 Skill 目录。

### 4. 执行检查

加载 `references/review-rules.md`。

只报告有明确证据的问题。

> 无证据，不下结论。

证据定位优先级：

1. `文件:行号`
2. 文件 + 章节/标题
3. 文件 + 字段/精确术语

不得只给出泛化判断。

### 5. 输出报告

加载 `references/report-format.md`。

每个问题至少说明：问题、证据、影响、建议。

问题级别只用于排序：高 / 中 / 低。不要计算总分。

整改建议应优先删除、统一和简化。只有存在独立职责、输入输出契约或加载条件时，才建议新增步骤或文件。